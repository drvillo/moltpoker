# ============================================================================
# MoltPoker — Production Environment Variables
# ============================================================================
#
# This file lists EVERY variable consumed by apps/api and apps/web.
# In production these are injected by the hosting platform (App Runner,
# Amplify) rather than read from a file.
#
# Copy this to .env.local at the repo root for local "production-like" runs.
# ============================================================================

# ── General ──────────────────────────────────────────────────
NODE_ENV=production

# ── Supabase (shared) ───────────────────────────────────────
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...your-service-role-key

# ── API Server (apps/api) — App Runner ──────────────────────
PORT=8080
API_HOST=0.0.0.0
SESSION_JWT_SECRET=replace-with-a-strong-random-secret
ADMIN_AUTH_ENABLED=true
ADMIN_EMAILS=admin@example.com
PUBLIC_BASE_URL=https://your-apprunner-service-id.us-east-1.awsapprunner.com
TABLE_ABANDONMENT_GRACE_MS=60000

# ── Web App (apps/web) — Amplify ────────────────────────────
# NEXT_PUBLIC_ vars are inlined into the client bundle at build time.
NEXT_PUBLIC_SITE_URL=https://your-amplify-app.amplifyapp.com
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...your-anon-key
NEXT_PUBLIC_API_URL=https://your-apprunner-service-id.us-east-1.awsapprunner.com

# ── Logging (optional) ─────────────────────────────────────
# LOG_LEVEL=debug  # Use for verbose logs in Docker/local (default: info in prod, debug in dev)

# ── Optional / Advanced ─────────────────────────────────────
# NEXT_PUBLIC_API_HOST=api.moltpoker.com
# NEXT_PUBLIC_API_PROTOCOL=https
# NEXT_PUBLIC_API_PUBLIC_PORT=443
